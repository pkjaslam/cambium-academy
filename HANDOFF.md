# HANDOFF — pick up here

_Last updated: 2026-07-12. Written so a fresh chat can continue with zero loss._

## Say this in the new chat
> Continue the Cambium Academy work. Read `CLAUDE.md`, `HANDOFF.md`, and `COURSE_PLAYBOOK.md`, then assemble the Course 3 lecture film and activate.

Project memory auto-loads the rest (Higgsfield pipeline, mount-truncation gotcha, AI-Lab game rule, per-course config rules). `CLAUDE.md` (repo root) is the Director's master instructions — read it first.

## Where the Academy stands
- **Course 01 — Intro to AI:** LIVE.
- **Course 02 — Prompting Essentials:** LIVE. (2026-07-12: repaired truncated tails of `capstone.html` + `career.html` — the mount had cut both mid-beacon during the 07-10 session.)
- **/start/ front door: REPAIRED 2026-07-12.** `start/index.html` had shipped TRUNCATED on 07-10 (file ended mid-FAQ inside the dc-logic script; no </script></body></html>). Live console showed `[dc-runtime] logic class eval FAILED for Root` and the cinematic hero logic was silently dead. Minimal repair: closed the structures after the last complete FAQ entry (no invented content; any FAQ entries beyond the four complete ones are lost). Logic parses again; heals live on next push. A full repo truncation sweep (every .html closes, every .js parses, every .json valid) now comes back clean.
- **Course 03 — Research with AI:** WEB COURSE COMPLETE, built 2026-07-12, audit PASSED. Awaiting the Director's push, then the lecture film.

## Course 3 — done (2026-07-12 session)
- Full web course in `courses/course-03-research-with-ai/web/`: index (video placeholder until film premieres), 40 slides + module checkpoints, transcript (~22 min narration, 3,312 words), 24 flashcards, 20-question quiz, AI Lab as 3 games (Citation Hunt / Lateral Race / Receipts or Vibes; station keys tok/lm/net preserved), capstone (research protocol + rubric), career guide, certificate CAMB-C03 (chips FIND/VERIFY/CITE WELL), badges JSON, community, AI Classroom wired on slides.html + classroom.html.
- Deck: `slides/Cambium-Academy-Course03-Research-with-AI.pptx` + `.pdf` (40 slides, generated from the live S array).
- `assets/social-course03.png` (matches the card set), og:image points at it.
- `/start/` Course 03 card flipped to LIVE + linked; `LINKS.md` Course 03 block added; `resources.md` created (all links live-verified 07-12; NOTE: newsliteracy.org is a parked domain now, we use newslit.org; checkplease.cc is dead, replaced with Caulfield's open textbook).
- `tools/audit-course.sh` made course-aware (residue exclusion `$SLUG`, C1 tripwire narrowed to "four prompt move" so SIFT's "four moves" doesn't false-positive).
- Configs byte-identical to Course 1. Every file dd-copied + cmp-verified. Full audit: PASSED.
- Staged classroom fixes (code-drawn diagram, at-capacity message) are included in the shipped `aira-classroom.js`.
- **AI Classroom v2 (2026-07-12, same session):** student-aware (reads slides/cards/misses/labs/quiz/cert from localStorage; personalized greeting, computed next-step, "Drill what I keep missing" chip fed by new `cambium-c03-cardmiss` tracking in flashcards.html), richer code-drawn visuals (Aira picks flow / concept map / comparison table / before-after via a JSON contract, safe fallbacks), spoken replies (browser speechSynthesis, per-reply button + persisted header toggle, honestly labeled), cross-page chat memory + 40-entry answer cache with 7-day TTL in `cambium-c03-classroom` (stretches the free quota). 30 automated checks pass; audit re-run PASSED.
- **Multi-provider worker v2.1 DEPLOYED LIVE (2026-07-12, with the Director driving approvals):** chain Gemini → Groq 70B → Groq 8B → Workers AI (keyless binding `AI`, added) → GitHub Models → Cohere → NVIDIA. Version 01deb562 on aira-chat.pkjaslamagrico.workers.dev; `GROQ_API_KEY` secret configured; end-to-end verified from the live site (`provider: gemini`, academy-wide prompt active, old MR_API_KEY honored). Source: `courses/course-03-research-with-ai/proxy/`. Capacity now ~15,000+ answers/day. NVIDIA rung ALSO LIVE (NVIDIA_API_KEY secret added 2026-07-12; free dev tier, integrate.api.nvidia.com). Director decided to KEEP the Groq key despite session transit (offered rotation, declined; same stance for the NVIDIA key). Optional remaining rungs: GH_MODELS_TOKEN, COHERE_API_KEY. The deferred fallback-router item is CLOSED.

## COURSE 3 v2 REDESIGN (Director's call, 2026-07-13) — THE NEXT BIG BUILD
The Director judged v1 too narrow (trust/citations only). `CURRICULUM.md` is now **v2**: the full 2026 research toolkit in 10 modules (deep research agents incl. Elicit/Undermind/Kosmos; AI coding assistants incl. Posit Assistant; multimodal figures/microscopy/handwriting; NotebookLM; knowledge graphs; AI peer review + policies; reproducibility + Zotero/BibTeX integrity; researcher prompting + responsible AI). v1 preserved as `CURRICULUM_v1.md`; its trust material becomes Module 3 and the 3 existing Lab games stay. All facts live-verified 2026-07-13 with a sources block in the file. Build notes inside: ~70 slides, Lab grows to 5 games (add ins/ps station keys, update all four reader files + gate to 5), same cert/keys, existing 7:20 film stays as the M3 featurette, full v2 film after rebuild. Rebuild order: slides+transcript → cards/quiz/labs → classroom contexts → deck/media → audit → push. **v2 IS COMPLETE AND SWAPPED IN (2026-07-13): the live web/ now IS v2** (60 slides/10 modules, 24 cards, 20 tagged quiz Qs, FIVE games, Vanta hero, classroom v2.1 with quiz-miss coaching, v2 transcript ~20:35, capstone=personal stack, cert chips FIND/VERIFY/CITE/DISCLOSE WELL, v2 deck pptx+pdf). AUDIT PASSED after swap; repo sweeps clean. Awaiting Director push (`push_academy.bat`). Post-swap extras DONE same day: social-course03.png refreshed to v2 messaging; film.js gained the v2 effects (FILM.kinetic/reveal/count, node-checked); **film v2 kit fully staged at `production/filmv2/`**: all 60 narration clips TTS'd (25:26 total, matches transcript word for word) + durations TSV + README with the exact safe-assembly recipe (single audio track, zero-joint video; the v1 desync lesson is written in). **FILM v2 RENDERED 2026-07-13: `production/filmv2/Cambium-Academy-Course03-v2-Full-Lecture.mp4` (28:00, 1080p24, 824MB, byte-verified on D:).** 66 segments (60 slides via ONE parametric renderer film_slide.js + 5 beats + sting), zero-joint assembly (audio 1680.876s vs video 1680.892s), 18 chapters + 298-cue SRT + description beside it. Kit: lib/film_slide.js + slides_data.js + entries/frames json in filmv2/. **FILM v3 (35:35, with the Riverbend thread) IS LIVE at https://youtu.be/vgZAYc14y4s and embedded in index.html; LINKS.md updated. The older 28:00 cut (OXhUOGckmPU) is superseded: unlist or delete it.** Previous state: index placeholder swapped for the embed; LINKS.md updated; audit re-PASSED. The 7:20 featurette remains available for social. FINAL STEP: Director runs push_academy.bat, then live-checks the course page, video playback, /start/ card, quiz gate, and classroom. /start/ card copy could optionally mention v2. Staging kept at `production/v2_staging/` as the build record. Complete + verified there: 60-slide 10-module slides.html, checkpoints (10), flashcards (24), quiz (20 tagged, balanced keys, missTopics for classroom drills), playground with FIVE games (ins/ps added), progress/aira readers on the 5-game 60-slide contract, index v2 with self-hosted Vanta hero, classroom v2.1 (quiz-miss coaching).  **`BUILD_SPEC_V2.md` locks the tech: Vanta.js heroes (scoped, self-hosted, mobile-safe), MotionForge-inspired film.js v2 effects (kinetic/reveal/count), 5-game Lab with ins/ps keys, Classroom v2.1 (quiz-miss drills + personal-stack builder), quality bar + file order inside. v2 lands complete or not at all; live v1 untouched until gauntlet passes.**

## Next steps, in order
1. **Push:** Director runs `push_academy.bat` (sandbox has no GitHub creds). Then live-check: /start/ card, course page, share image resolves, quiz gate, classroom answers + personalized greeting.
2. Optional: add GH_MODELS_TOKEN / COHERE_API_KEY rungs; rotate Groq/NVIDIA keys anytime if wanted.
3. **FILM ASSEMBLED 2026-07-13: `production/film/Cambium-Academy-Course03-Research-with-AI-Film.mp4` (7:21, 1080p24, 62MB).** 18 segments: 5 Higgsfield Aira beats + 13 code-rendered cinematic segments (film.js kit, edge-tts Neerja voice, 9,300 deterministic frames via chromium headless shell). SRT + CHAPTERS.txt + YOUTUBE_DESCRIPTION.txt sit next to it; full re-render kit in `production/film/build/`. QA: frames sampled across all segment types, audio levels verified continuous.
4. **Director: review the film, upload to YouTube** (unlisted → review → public, paste YOUTUBE_DESCRIPTION.txt, upload the SRT), then swap the index.html placeholder for the embed (placeholder block is clearly marked), update LINKS.md video line, re-sync transcript timings to the film if desired. The `cambium-c03-lecture` flow stays as-is.

## Hard-won gotchas (do not relearn these)
- **RUNTIME/CONTENT CHANGE = SWEEP ALL DOWNSTREAM ARTIFACTS.** If the film length or content changes, update ALL of: thumbnail (it bakes in the runtime), YouTube title, description+chapters, SRT, the site embed, index copy, LINKS.md. If slide count changes, update: checkpoints indices+total, quiz/progress/aira/classroom/playground gates, index "read all N slides", transcript. If Lab games change: playground+quiz+progress+aira+slides nextStep TOGETHER. Missing one of these is how stale copy reaches students.
- 2026-07-13 consistency sweep found + fixed: cert-extras share text (still v1 pitch), badge JSON (still "three AI Lab stations"), resources.md (v1 tool list, now a v2 ten-instrument page), The Practical unreachable from 10 pages (now in every nav + the quiz pass screen + certificate bar), /start/ catalog blurb still v1 ("90 min - beginner" -> "3 hours - researchers"), thumbnail said 28 min (film is 35).
- 2026-07-13 FINAL SESSION ADDS (Director's top-5 items 1/2/3): **The Practical** (`practical.html`): optional honors exam, Part A grades 5 citations against the LIVE Crossref API in-browser (real DOIs verified: 3 resolve, 2 fabricated 404), Part B is an agent-report load-bearing-source audit graded by Aira (with answer-key fallback when offline); passing writes `cambium-c03-practical` and stamps **FIELD-VERIFIED** on the certificate (both HTML + canvas renders). **The Researcher's Pack** (`pack.html`): copy-to-clipboard prompt cards, per-venue disclosure statements (Science/Nature/Elsevier/NIH), 3-click check + data-sensitivity tree + stakes table, personal-stack template, print-to-PDF CSS. **The Riverbend thread**: 10 case-study slides woven through the deck (60 -> 70 slides), one per module, following one real research question end to end; checkpoints re-indexed to 10/17/25/32/37/43/50/56/62/66 and ALL slide counts updated to 70 across quiz/progress/aira/classroom/playground/index. Audit PASSED.
- **DIRECTOR'S STANDING RULE (2026-07-13): features from Course 1/2 are ADDED TO or MODIFIED, never removed.** QR + cert ID + verify link + badge + LinkedIn + print CSS are inviolable certificate features. Also: the certificate page has TWO renders (on-page HTML + canvas download in drawCert) that must be edited in lockstep, and the cert content box is FIXED-HEIGHT with overflow:hidden, so in-flow additions clip the ID/QR zone; add decorations as absolutely-positioned elements and reclaim margins for flow additions. Cert v2 upgrades shipped: 4 WELL chips both renders, honors/score line, banknote microprint band, COURSE 02 residue fixed in canvas, QR/date collision fixed.
- 2026-07-13 late: AI Lab got the full arcade treatment (cinematic hero, quiz-credit meter, 5-cabinet lobby wired to pgState, upgraded game chrome; hud() also paints lobby+meter; emoji buttons replaced with text for glyph safety). classroom.html preview-era copy fully purged (kicker + lesson block now v2). parseSteps hardened: only real list lines become diagram steps. All shipped + audit PASSED. Everything awaits ONE push.
- **The Aira worker slices EACH message to 2,400 chars.** Never send lesson context + request as one message: v2's bigger AIRA_LESSON pushed SYS alone to 2,404 chars, silently amputating every request (classroom answered with friendly greetings that quoted the lesson back). Fixed 2026-07-13: aira-classroom.js sends [user: context, assistant: ack, user: request] as separate messages, and AIRA_LESSON was compacted. Live-verified: visual ask returns diagram JSON, example ask returns worked examples.
- **The D: mount silently truncates writes.** Author big in /tmp, dd bs=4M to repo, then cmp. It truncated two C02 pages last session; found + repaired this session.
- **/tmp files can persist across sessions read-only (owner nobody).** Never reuse old /tmp filenames; a stale root-owned file once nearly overwrote a repo tool. Use unique names, verify content before dd.
- **Aira's worker is on Gemini's free tier (~1,500 req/day)** and hits a daily cap; multi-provider fallback router (Gemini → Groq → GitHub Models → Cohere → NVIDIA) is deferred; the Director asked to be reminded.
- **Higgsfield credits nearly gone (~6 left),** trial ending, auto-renew cancelled. The 5 Course-3 beats are already rendered locally, so film assembly costs nothing.
- Every AI Lab must feel like a **game** (this one does: scores, streaks, timers, stamps, confetti).
- Never let earlier-course content leak forward. The audit's residue check is now slug-aware.
